# Server Configuration
PORT=5000
NODE_ENV=development

# MySQL Database Credentials
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=rootpassword
DB_NAME=school_cms

# Security & Session Secret
SESSION_SECRET=school_admin_secret_key_change_in_production_2026

# Storage Driver ('local' for dev, 's3' or other providers in production)
STORAGE_DRIVER=local

# CORS Allowed Origin
FRONTEND_URL=http://localhost:5000
